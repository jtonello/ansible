---
- name: Hello World!
  hosts: all
 
  tasks:
 
  - name: Hello World!
    shell: echo "Hi! Tower is working."
    
  - name: Install ntp
    apt: name=ntp state=installed 
    tags: ntp
    become: true
