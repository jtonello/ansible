---
- name: Hello World!
  hosts: all
 
  tasks:
     
  - name: Hello World!
    shell: echo "Hi! Tower is working."
    
  - name: Install ntp
    sudo: yes
    apt: name=ntp state=installed 
    tags: ntp

